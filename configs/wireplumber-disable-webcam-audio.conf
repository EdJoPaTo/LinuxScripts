# See https://wiki.archlinux.org/title/WirePlumber
monitor.alsa.rules = [
  {
    matches = [
      {
        device.form-factor = "webcam"
      }
    ]
    actions = {
      update-props = {
        device.disabled = true
      }
    }
  }
]
